// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyAHNsQFjjYTTTZ2xFsww_XlA4fFatHTBys",
    authDomain: "work-hours-pwa.firebaseapp.com",
    databaseURL: "https://work-hours-pwa.firebaseio.com",
    projectId: "work-hours-pwa",
    storageBucket: "work-hours-pwa.appspot.com",
    messagingSenderId: "693007638898",
    appId: "1:693007638898:web:89ae3391aeb667d2f9fdeb"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
